tmp.r[-c(cells)] <- new_dist
max.diff<-which.max(tmp.r)
cells<-c(cells, max.diff)
return(cells)
}
cells
long=-75.972468
lat= 42.083805
num=10
data=rast_mat
stack=r_stack
# create ref layer
ref<-stack[[1]]
values(ref)<-NA
# first site selection
cell1<-terra::cellFromXY(ref, cbind(long, lat))
cell2<-max_cell(first_distance(data))
cells<-c(cell1, cell2)
cells
# use cell 1 and 2 to pick other sites, each iteration will calculate a new distance matrix between chosen and unchosen cells.
for (i in 1:num){
new_dist<-new_distance(data, cells)
tmp.r<-as.matrix(ref)
tmp.r[!is.na(tmp.r)] <- NA
tmp.r[-c(cells)] <- new_dist
max.diff<-which.max(tmp.r)
cells<-c(cells, max.diff)
return(cells)
}
cells
# use cell 1 and 2 to pick other sites, each iteration will calculate a new distance matrix between chosen and unchosen cells.
for (i in 1:num){
new_dist<-new_distance(data, cells)
tmp.r<-as.matrix(ref)
tmp.r[!is.na(tmp.r)] <- NA
tmp.r[-c(cells)] <- new_dist
max.diff<-which.max(tmp.r)
cells<-c(cells, max.diff)
}
cells
source("C:/Users/joeel/SiteSelectR/R/site_select.R")
source("C:/Users/joeel/SiteSelectR/R/site_select.R")
source("C:/Users/joeel/SiteSelectR/R/site_select.R")
source("C:/Users/joeel/SiteSelectR/R/site_select.R")
site_select<-function(long, lat, num,
data, stack){
# create ref layer
ref<-stack[[1]]
values(ref)<-NA
# first site selection
cell1<-terra::cellFromXY(ref, cbind(long, lat))
cell2<-max_cell(first_distance(data))
cells<-c(cell1, cell2)
# use cell 1 and 2 to pick other sites, each iteration will calculate a new distance matrix between chosen and unchosen cells.
for (i in 1:num){
new_dist<-new_distance(data, cells)
tmp.r<-as.matrix(ref)
tmp.r[!is.na(tmp.r)] <- NA
tmp.r[-c(cells)] <- new_dist
max.diff<-which.max(tmp.r)
cells<-c(cells, max.diff)
}
return(cells)
}
site_select<-function(long, lat, num,
data, stack){
# create ref layer
ref<-stack[[1]]
values(ref)<-NA
# first site selection
cell1<-terra::cellFromXY(ref, cbind(long, lat))
cell2<-max_cell(first_distance(data))
cells<-c(cell1, cell2)
# use cell 1 and 2 to pick other sites, each iteration will calculate a new distance matrix between chosen and unchosen cells.
for (i in 1:num){
new_dist<-new_distance(data, cells)
tmp.r<-as.matrix(ref)
tmp.r[!is.na(tmp.r)] <- NA
tmp.r[-c(cells)] <- new_dist
max.diff<-which.max(tmp.r)
cells<-c(cells, max.diff)
}
return(cells)
}
sites<-site_select(-75.972468, 42.083805, 10, rast_mat, r_stack)
sites
plot_sites<-function(cells, stack){
site_xy<-terra::xyFromCell(stack[[1]], cells)
plot(stack[[1]])
points(site_xy, pch=19, cex=1.5, col='red')
}
plot_sites<-function(cells, stack){
site_xy<-terra::xyFromCell(stack[[1]], cells)
plot(stack[[1]])
points(site_xy, pch=19, cex=1.5, col='red')
return(site_xy)
}
# plot_sites() -----------------------------------------------------------------
xy_sites<-plot_sites(sites)
# plot_sites() -----------------------------------------------------------------
xy_sites<-plot_sites(sites, r_stack)
site_val<-function(sites, stack){
site_val<-terra::extract(r_stack, xy) %>% data.frame()
return(site_val)
}
# site_val() -------------------------------------------------------------------
site_values<-site_val(xy_sites, r_stack)
site_values
xy_sites
source("C:/Users/joeel/SiteSelectR/R/site_select.R")
site_val<-function(sites, stack){
site_val<-terra::extract(stack, xy) %>% data.frame()
return(site_val)
}
# site_val() -------------------------------------------------------------------
site_values<-site_val(xy_sites, r_stack)
site_values
site_val<-function(sites, stack){
site_val<-matrix(data.frame(terra::extract(stack, sites)))
return(site_val)
}
# site_val() -------------------------------------------------------------------
site_values<-site_val(xy_sites, r_stack)
site_values
site_val<-function(sites, stack){
site_val<-data.frame(terra::extract(stack, sites))
return(site_val)
}
# site_val() -------------------------------------------------------------------
site_values<-site_val(xy_sites, r_stack)
site_values
site_val<-function(sites, stack){
site_val<-data.frame(terra::extract(stack, sites))
sites_mat<-matrix()
return(site_val)
}
# site_val() -------------------------------------------------------------------
site_values<-site_val(xy_sites, r_stack)
site_values
vis_rep<-function(values, stack, layer1, layer2){
plot(stack[[layer1]], r_stack[[layer2]], col="lightgray")
points(site_val[,layer1], site_val[,layer2], col="red", pch=19, cex=1.5)
}
# vis_rep() --------------------------------------------------------------------
vis_rep(site_values, r_stack, 1, 2)
vis_rep<-function(values, stack, layer1, layer2){
plot(stack[[layer1]], r_stack[[layer2]], col="lightgray")
points(values[,layer1], values[,layer2], col="red", pch=19, cex=1.5)
}
# vis_rep() --------------------------------------------------------------------
vis_rep(site_values, r_stack, 1, 2)
points(values[,layer1], values[,layer2], col="red", pch=19, cex=1.0)
vis_rep<-function(values, stack, layer1, layer2){
plot(stack[[layer1]], r_stack[[layer2]], col="lightgray")
points(values[,layer1], values[,layer2], col="red", pch=19, cex=1.0)
}
# vis_rep() --------------------------------------------------------------------
vis_rep(site_values, r_stack, 1, 2)
# vis_rep() --------------------------------------------------------------------
vis_rep(site_values, r_stack, 2, 4)
# Calculate the correlation matrix
cor_matrix <- cor(site_values)
cor_matrix
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("green", "white", "red"))(20)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
main = "Correlation Heatmap of mtcars",
margins = c(8, 8)) # Adjust margins for labels
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(20)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
main = "Correlation Heatmap of mtcars",
margins = c(8, 8)) # Adjust margins for labels
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8)) # Adjust margins for labels
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(12)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8)) # Adjust margins for labels
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(10)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8)) # Adjust margins for labels
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(5)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8)) # Adjust margins for labels
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(50)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8)) # Adjust margins for labels
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(100)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8)) # Adjust margins for labels
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(300)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8)) # Adjust margins for labels
# Define a color palette for the heatmap
# Green for negative, white for zero, red for positive correlations
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(100)
# Create the heatmap
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8)) # Adjust margins for labels
cor_plot<-function(values){
cor_val<-cor(values)
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(100)
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8))
}
cor_plot<-function(values){
cor_val<-cor(values)
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(100)
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8))
return(cor_val)
}
# Calculate the correlation matrix
cor_matrix <- cor(site_values)
cor_vis<-function(values){
cor_val<-cor(values)
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(100)
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8))
return(cor_val)
}
vis_cor<-function(values){
cor_val<-cor(values)
palette <- colorRampPalette(c("#56B4E9", "white", "orangered"))(100)
heatmap(cor_matrix,
col = palette,
symm = TRUE, # Ensures symmetry for correlation matrix
margins = c(8, 8))
return(cor_val)
}
# vis_cor() --------------------------------------------------------------------
vis_cor(site_values)
plot(r_stack)
# vis_cor() --------------------------------------------------------------------
vis_cor(site_values)
setwd("C:/Users/joeel/SiteSelectR")
devtools::document()
devtools::document()
usethat::use_vignette("SiteSelectR_Intro")
devtools::use_vignette("SiteSelectR_Intro")
usethat::use_vignette("SiteSelectR_Intro")
library(usethis)
usethis::use_vignette("SiteSelectR_Intro")
devtools::load_all()
devtools::load_all()
library(SiteSelectR)
## continuous rasters
cont1 = matrix(nrow=20, ncol=20, seq(1:400)) %>% rast(crs="EPSG:4326") %>%
terra::project("EPSG:4326")
cont2 = matrix(nrow=20, ncol=20, seq(1:400)) %>% rast(crs="EPSG:4326") %>%
terra::project("EPSG:4326")
n=5
class_rast<-list()
for (i in 1:n){
r = rast(nrows=20, ncols=20, xmin=0, xmax=20, ymin=0, ymax=20,
crs="EPSG:4326")
values(r)<-sample(200, ncell(r), replace=T)
class_rast[[i]]<-r
}
class_rast<-rast(class_rast)
class_rast
devtools::document()
random_rast<-rast(class_rast)
r_list<-SiteSelectR::rast_collect(random_rast, cont1, cont2)
devtools::load_all()
r_list<-SiteSelectR::rast_collect(random_rast, cont1, cont2)
plot(cont1)
library(SiteSelectR)
r_list<-SiteSelectR::raster_collect(random_rast, cont1, cont2)
devtools::install(SiteSelectR)
devtools::install("SiteSelectR")
setwd(..)
setwd("..")
devtools::install("SiteSelectR")
library(SiteSelectR)
r_list<-SiteSelectR::rast_collect(random_rast, cont1, cont2)
r_list<-SiteSelectR::rast_collect(random_rast, cont1, cont2)
r_list<-SiteSelectR::rast_collect(random_rast, cont1, cont2)
library(SiteSelectR)
library(SiteSelectR)
The SiteSelectR library works with the terra and sf  libraries. Both are used in SiteSelectR to handle raster data (terra) and vectorized shapefile data (sf).
library(SiteSelectR)
devtools::install("SiteSelectR")
library(SiteSelectR)
library(terra)
library(terra)
library(sf)
cont1 = matrix(nrow=20, ncol=20, seq(1:400)) %>% rast(crs="EPSG:4326") %>%
terra::project("EPSG:4326")
cont2 = matrix(nrow=20, ncol=20, seq(1:400)) %>% rast(crs="EPSG:4326") %>%
terra::project("EPSG:4326")
n=5
class_rast<-list()
for (i in 1:n){
r = rast(nrows=20, ncols=20, xmin=0, xmax=20, ymin=0, ymax=20,
crs="EPSG:4326")
values(r)<-sample(200, ncell(r), replace=T)
class_rast[[i]]<-r
}
random_rast<-rast(class_rast)
r_list<-SiteSelectR::rast_collect(random_rast, cont1, cont2)
devtools::load_all()
r_list<-rast_collect(random_rast, cont1, cont2)
r_list
cont1
extent<-sample_ext(x=3.5, y=13.3, 50000)
extent
extent<-sample_ext(x=3.5, y=13.3, 50000)
extent<-sample_ext(x=3.5, y=13.3, 50000)
extent
plot(random_rast$lyr.1)
plot(cont1)
lines(extent, col='red')
plot(cont1)
lines(extent, col='red')
plot.new(lines(extent, col='red'))
plot(cont1)+lines(extent, col='red')
extent<-sample_ext(x=3.5, y=13.3, 500000)
extent<-sample_ext(x=3.5, y=13.3, 500000)
plot(cont1)+lines(extent, col='red')
extent<-sample_ext(x=3.5, y=12.0, 500000)
extent<-sample_ext(x=3.5, y=12.0, 500000)
```{r}
```{r}
```{r}
extent<-sample_ext(x=3.5, y=12.0, 500000)
plot(cont1)+lines(extent, col='red')
extent<-sample_ext(x=10.0, y=12.0, 500000)
```{r}
plot(cont1)+lines(extent, col='red')
plot(cont1)
lines(extent, col='red')
+
plot(cont1)
plot(cont1)
lines(extent, col = "red")
plot(cont1)
plot(extent, add = TRUE, col = "red")
plot(cont1)+ plot(extent, add = TRUE, col = "red")
plot(cont1)+ lines(extent, add = TRUE, col = "red")
plot(cont1)
lines(extent, col = "red")
plot(cont1)
lines(extent, col = "red")
plot(cont1) + lines(extent, col = "red")
plot(cont1)
lines(extent, col = "red")
plot(cont1)+lines(extent, col = "red")
plot(cont1)+lines(extent, col = "red")
ggplot2::ggplot()+geom_rast(data=cont1)+geom_sf(data=extent)
ggplot2::ggplot()+geom_raster(data=cont1)+geom_sf(data=extent)
library(ggplot2)
ggplot2::ggplot()+geom_raster(data=cont1)+geom_sf(data=extent)
plot(cont1, extent)
plot(cont1)
lines(extent, col='red')
plot(cont1)+lines(extent, col='red')
plot.new()
lines(extent, col='red')
plot.new(lines(extent, col='red'))
extent<-sample_ext(x=10.0, y=12.0, 750000)
lines(extent, col='red')
plot(cont1)
plot(extent, add = TRUE, col = "red")
extent
plot(cont1)
coords <- sf::st_coordinates(extent)
lines(coords[,1], coords[,2], col = "red")
library(SiteSelectR)
knitr::opts_chunk$set(
collapse = TRUE,
comment = "#>"
)
library(SiteSelectR)
library(sf)
cont1 = matrix(nrow=20, ncol=20, seq(1:400)) %>% rast(crs="EPSG:4326") %>%
terra::project("EPSG:4326")
library(terra)
new("standardGeneric", .Data = function (x, ...)
standardGeneric("project"), generic = "project", package = "terra",
group = list(), valueClass = character(0), signature = "x",
default = NULL, skeleton = (function (x, ...)
stop(gettextf("invalid call in method dispatch to '%s' (no default method)",
"project"), domain = NA))(x, ...))
cont1 = matrix(nrow=20, ncol=20, seq(1:400)) %>% rast(crs="EPSG:4326") %>%
terra::project("EPSG:4326")
cont2 = matrix(nrow=20, ncol=20, seq(1:400)) %>% rast(crs="EPSG:4326") %>%
terra::project("EPSG:4326")
n=5
class_rast<-list()
for (i in 1:n){
r = rast(nrows=20, ncols=20, xmin=0, xmax=20, ymin=0, ymax=20,
crs="EPSG:4326")
values(r)<-sample(200, ncell(r), replace=T)
class_rast[[i]]<-r
}
random_rast<-rast(class_rast)
extent<-sample_ext(x=10.0, y=12.0, 750000)
load_all()
library(devtools)
devtools::load_all()
extent<-sample_ext(x=10.0, y=12.0, 750000)
lines(extent, col='red')
plot(cont1) ; lines(extent, col='red')
library(SiteSelectR)
n=3
for (y in 1:n) {
r1 <- terra::rast(matrix(rnorm(400, mean = 0, sd = 1),
nrow = 20, ncol = 20), crs = "EPSG:4326")
r_stack[[y]] <- r1
}
r1<-terra::rast(matrix(rnorm(400, mean=0, sd=1),
nrow=20, ncol=20), crs="EPSG:4326")
r2<-terra::rast(matrix(rnorm(400, mean=0, sd=1),
nrow=20, ncol=20), crs="EPSG:4326")
r3<-terra::rast(matrix(rnorm(400, mean=0, sd=1),
nrow=20, ncol=20), crs="EPSG:4326")
r3
extent<-sample_ext(x=10.0, y=12.0, 750000)
devtools::use_data()
library(devtools)
devtools::load_all()
extent<-sample_ext(x=10.0, y=12.0, 750000)
library(terra)
library(sf)
extent<-sample_ext(x=10.0, y=12.0, 750000)
extent
extent<-sample_ext(x=10.0, y=12.0, 750000)
head(extent)
plot(r1) ; lines(extent, col='red')
r_list<-rast_collect(r1, r2, r3)
r_list
head(r_list)
r_resampled<-rast_match(r_list)
sampling_extent<-sample_ext(x=10.0, y=12.0, 750000)
head(sampling_extent)
r_resampled<-rast_match(raster_list = r_list,
reference_layer = r_list[1],
extent = sampling_extent)
r_resampled<-rast_match(raster_list = r_list,
reference_layer = r_list[1],
extent = sampling_extent)
r_resampled<-rast_match(raster_list = r_list,
reference_layer = r_list[[1]],
extent = sampling_extent)
head(r_resampled)
r_stack<-rast_stack(r_resampled)
plot(r_stack)
names(r_stack)<-c("Raster 1", "Raster 2", "Raster 3")
plot(r_stack)
devtools::document()
r_resampled<-rast_match(raster_list = r_list,
reference_layer = r_list[[1]],
extent = sampling_extent)
head(r_resampled)
Use rast_match() to resample the raster list to the same extent, resolution, and CRS.  rast_match() takes the product of r_list() - a list of rasters and resamples them following the extent, resolution, and CRS of a reference raster().
r_stack<-rast_stack(r_resampled)
r_stack<-rast_stack(r_resampled, sampling_extent)
names(r_stack)<-c("Raster 1", "Raster 2", "Raster 3")
plot(r_stack)
document()
document()
devtools::document()
