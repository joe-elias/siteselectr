#' Combine individual cells into a list to use in the site_select() function.
#' @param cells any number of cells as values.
#' @return description input any number of cell objects to combine into a list.
#'
#' @export

cell_combine<-function(...){
  cells_comb<-c(...)
}

